# Deploying TextApp

> The full, tailored deployment guide for your Oracle VPS (VS Code Remote-SSH, your VPS IP, port 9090, Cloudflare Tunnel) lives at the repo root:
>
> **[DEPLOY.md](../DEPLOY.md)**

That guide covers: connecting with VS Code Remote - SSH, installing Node 22, uploading the `server/` folder, configuring `.env`, running the app as a systemd service on port 9090, hiding your IP with Cloudflare Tunnel, an optional direct-IP test build, and troubleshooting.
